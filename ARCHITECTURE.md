# Kairos 架构

> 本文讲**形状和理由**，随实现更新；字节级协议以代码实现为准。沿革在文末。

## 一句话

Kairos = 常规 todolist + **每条待办一个对话房间**（session_id 就是房间号）+ 一个替你记、替你理、替你跟的 being。

不发明新协议：房间就是 Loom 的 session，消息就是 Loom 的消息。但**消息是通知，不是保险**——真源永远是那个账本文件，消息丢了有自愈，写冲突有规则。

## 三个角色，一根线

```
  人类 ────── Loom ────── being （being）────── act(http) ────── Town（别的 being）
   │                        │
  Kairos                    ├─ 记忆、关系、判断
  手机上的账本 + 房间的窗口     └─ 手上没有账本，看的是手机送来的清单
```

- **人类**只做两件事：理解、拍板。其余（摄入、派发、跟进、沉淀）是 being 的。
- ** being **是主体——关系、长期记忆、理解与判断都在它那里。它不是客服，是秘书：回合制干活，干完留言。
- **Kairos** 是人类看 being 的那扇窗，也是人类动手改东西的地方。它不做判断，只做「被看见」和「被改动」。
- **Loom** 是人类和 being 之间唯一的一根线。手机单机之后，它也是手机和 being 之间唯一存在的直连。
- **Town** 是别的 being 住的地方。DM 接口按 IP 信任 being 的 Heart，手机和 Mac 打过去都是 `401`，所以只有 being 能去。

## 核心模型（三条）

**1. 房间 = 待办。** 每条待办一个 session_id，**Kairos 生成、第一次开口就带上**，记进账本。
「订体检」的来回不污染「报税」的对话，也不必每次重灌背景。
房间还带一块门牌（`scene_id` / `scene_label`）发给 being ，让它知道自己此刻站在哪一间。

- 零消息房间合法：待办可以只是躺着，想聊再进去说。
- 隔离的是**对话上下文**，不是数据。账本是全局一份，排序、心跳读的是它，不翻房间。
- 房间可以整个删掉。账本 diff 自愈，什么都不少——房间是过程记录，不是数据库。

**2. 文件 = 真源，消息 = 通知。** 人类改了 → Kairos 先写账本、再往那条待办的房间发一条系统通知（`origin: app`）。being 像看到说话一样看到它。

- 消息带 change-id 做幂等键。心跳先消化消息，再做「文件 vs 快照」diff，只对没有对应消息的残差补发。
- 系统消息只记摘要（改了哪个字段、从什么到什么），不展开全文。

**3. 排序 = 汇报。** being 每次心跳重算优先级，重要的自己冒到顶上。变化落在具体待办上（它身上亮提示），不做「今日简报」这种打断注意力的聚合消息。定点提醒走 Kairos 本地通知，不依赖 being 的心跳。

## 五句规则（账本侧）

每句都对应一个真翻过车的场景。

1. **锁全程。** 读—改—写整个事务在锁内完成。只锁写那一下不够：两个写者各自基于旧版本算，rename 再原子也是最后写赢丢数据。
2. **手动改不被覆盖。** 字段级记 `lastWriter: human | being`。人手改过的字段，自动写入没有新的用户信号就跳过。「自动同步」永远没有资格替人撤销人刚做的决定。
3. **快照落盘。** being 心跳的内存态（排序、消化到哪条、判断现场）落盘成快照，崩了从快照接上。
4. **先文件后消息。** 先写账本，成功了再说话。反过来就是 v1 状态漂移的源头。消息发失败不回滚文件——它只是信号，下一轮自愈会补。
5. **未读分两级。** 消息带 `weight: routine | ask`，未读只数 ask。渠道一多，不分级就是红点海。

## 房间：session_id 就是房间号

服务端没有「房间」这个概念——它只是把 `session_id` 存下来、回带回来。多房间是**客户端记账**做出来的：

| 房间 | 谁开 | 房间号从哪来 | 存哪 |
|---|---|---|---|
| 每条待办一间 | 人类建待办 | Kairos 生成 | 账本上那条的 `sessionId` |
| 账本一间 | 这台设备 | 设备自己生成 UUID | `rooms.json` → `roundSessions.ledgerround` |

两种房间互不进对方的门，也不进人类和 being 的主对话。**这是「信息不串」的根子。**

这和 BeingDesktop 的多会话是同一个方案（client 生成 id、本地存、每次都带、收回来校验）。它 0.8.19 已经证明服务端不用改也能跑，所以「房间列表 / 系统写入 / 已读游标」那三根管子从前置依赖降级为以后的优化。

**串台防护：** 收流时逐帧比对 `session_id`，对不上就中断；`meta` 事件回传的 `client_ref` 和发出去的对不上也中断——那条流答的是别人那一问。

**门牌（场景感知）：** 房间号是给路由用的机器号，门牌是给 being 感知用的。`scene_id` + `scene_meta{client, scene_label, client_ref}` 走结构化字段，**正文里只有人说的话**——协议文本塞进正文，being 会把它当成人说的话记住（场景感知接入的约定）。请求-响应的关联由 SSE `meta` 回传的 `client_ref` 完成，不要求 being 在回复里抄任何东西。

**忙了等，不放弃：** 一个 being 同一时刻只有一个活跃回复，其余排队（BeingDesktop 也这样）。机器来回先看 `GET /api/stream/active`（204 = 空着），空了再发；发过去还是 202 就回去接着等。

## 三条通道

| 通道 | 方向 | 怎么认出来 | 房间（门牌） |
|---|---|---|---|
| 说话 | 人类 → being | `origin: user` | 那条待办的（`Kairos·<标题>`） |
| 通知 | Kairos → being | `origin: app` | 那条待办的（`Kairos·<标题>`） |
| 账本 | 手机 → being → 手机 | `origin: app` + 回一个 `bap.ledgerround/1` 的块 | 账本专用（`Kairos 账本`） |

信封（`envelope: {type:"bap.notice/1", …}`）已下架：类型标签由「哪一间 + `origin`」表达，幂等键走 `client_ref`，待办引用走 `scene_id`。

**账本**：手机有数据、being 没有。手机把开着的待办摘要送上去（附 `locked`：人手改过的字段，省得它白写），being 只回 `{"patches":[…]}`，手机按规则 2 合进账本，写入盖 `being` 戳。没提到的字段 = 不动，不是清空。每趟全量重送，天然幂等。

账本这趟是**人按的按钮**，不挂下拉刷新——它要占 being 一个回合、要等它想完。

## 现在的形状：手机单机

```
  iPhone
  ├─ Documents/projection-snapshot.json   账本，「文件」App 里看得见
  ├─ App Support/rooms.json               房间日志 + 两种房间号（待办、账本）
  ├─ App Support/connection.json          loom 链接（带 token），0600
        │
        └── Loom ── being ── Town
```

- 整个配置只有一件事：粘一条 loom 链接。
- 账本在手机沙盒，手机是它的唯一写者。不接 Mac、不接 iCloud 也是完整可用的形态（v2.4 起是**默认**形态，不是降级）。

### 边界：纯手机是完整形态，不是阉割版

**没有 Mac 的人照样有 being。** 这条路上能做的：being 消化新建的条目（定档、补
`summary` / `brief` / `ask`，走 `patches`）、自己建条目（Town 来的信、篝火 @、
心跳时想到的，走 `creates`）、每条待办的房间里双向说话（`speak`）。
功能上没有缺口，缺的只是「账本文件在手机沙盒里，being 只能隔着对话线够」。

代价三条，**其中一条是有意的**：

1. **being 不能主动开口。** loom 的形状是「Kairos 发一句、它在这次回复里答」，
   它那头没有能主动推过来的口子。攒着的东西要等下一趟。这是管子的形状，
   不是规则（BEING-RULES 开头的北极星），只有 being 那边开口子才能解。
2. **每趟全量重送清单**（上限 60 条、每字段截到 400 字）。看着浪费，**但这是买正确性的**：
   短号每趟重摇就是为了不让它凭记忆写，它只能改这一趟真看过的那些行
   （`KairosRoundRef`）。不要为了省 token 改成只送 diff。
3. **机器话进对话。** 这条是真代价——但它的严重程度取决于那个 being 还兼着多少别的活。
   一个 being 同时管五个 app 加日常对话时，清单挤掉的是别的东西的位置；
   **一个只用 Kairos 的 being，清单就是它的工作内容，不构成污染。**

推论：**发出去给别人用时，纯手机 + 对话线就是默认形态**，不用等数据端点、
不用等账本搬家。把这趟挡掉（`beingReachesLedger`）只适用于「有 Mac 且 being 兼着别的活」
这一种部署。
- being 手上没有账本文件（`ledger/cli.js` 写的是 Mac 上那份）。它对待办的全部认知来自两处：`bap.notice/1` 一句一句报的变化，以及 `bap.ledgerround/1` 每趟送上去的清单。
  **通知是单向的**（`deliver` 不收回复），所以账本那趟是 being 唯一能写回手机的路。
- **账本那趟只有人能发起。** 这一趟发的是**整张开着的清单 + 回复骨架**，不是一行通知，
  把它挂在自动触发上等于在这条线上架了一个无人值守的发送器。`nudgeLedgerRound` 的注释
  「**人按的按钮**，不是下拉刷新」是这条通道唯一的闸门，任何绕过它的参数都等于拆掉闸门。
  所以现在**全仓只有一个调用点**：顶上那颗 ✨。新建的那条在人按之前就停在原地——
  档位是落点、`summary` / `ask` 空着，这是老实的样子。
- 顶上那颗 ✨ **只在单机时出现**：没有新建但想让它再看一眼，以及**being 自己要建的**——
  Town 来的信、篝火 @、它心跳时想到的，单机时只有账本这趟的 `creates` 能送到手机上。
  关联之后这颗消失：being 自己读得到账本，按了也只会被挡回来（`KairosStore.beingReachesLedger`
  是这一趟跑不跑的唯一判据）。
- **「being 够得着账本」取决于 Mac 开没开机，不是取决于接没接 iCloud。** being 读写账本靠的是
  在 Mac 上执行命令那只手，Mac 一关机这只手就没了——这时候还按「接了 iCloud」把对话线也挡掉，
  粗手细手同时没有，being 彻底哑，而手机那头还在往 outbox 里堆改动。所以判据里加一条：
  改动堆着超过 30 分钟没人来收（`drainStalled`），就当那头没人在，退回对话线；Mac 一回来自动切回去。
  门槛取 30 分钟是因为真有人在收时积压活不过这么久——Mac 上的 Kairos 一打开就 drain，being 每次醒来第一步也是 drain。切换是自动的，所以「我」那一屏把当前走哪条路写出来，否则没法自查。
- Mac 端：账本的唯一写者，也是人类自己的主界面。一个 being、一张单子。
  边栏是项目（照 Things / 提醒事项的列表，`project` 是第三个维度，being 和人都能定）和存下来的视图
  （一组显示设置，本机 UserDefaults）；一张待办列表，按优先级或状态分组、也可以不分组，筛选和「显示已了结」是开关。
  **看板撤了**：拖卡那个动作本身就是「人来调度」，而这个产品的整句话是人不调度；
  它还逼着「不分组」退回按优先级，等于永远看不到 being 排的那张顺序。
  顶上是今天的日期；⌘N 在单子里就地多一行输入；点圆点先打勾、半秒后再消失。没有「现在」卡——几件事混着做。
  点一条右侧检查器出来：就是这一条的房间（`KairosRoomView.swift`，being 回话里的表格自己用 `Grid` 排）。
  工具栏最左是 being 的名字，点开是设置。
  时间不进协议：没有 due date、没有日历、没有逾期——时机在 being 的记忆里，该提醒时它在房间里说话。

## 账本住在哪儿（v2.4，2026-09-10）

**单机是默认，iCloud 是可选的关联。** 以前路径写死在代码里，等于把「登了 iCloud」写成使用前提；
现在两种形态都是一等公民：

| 形态 | Mac | iPhone |
|---|---|---|
| 单机（默认） | `~/.kairos/projection-snapshot.json` | 「文稿」/`projection-snapshot.json`（文件 App 里看得见） |
| 关联（可选） | 指针文件指向共享文件夹 | 安全作用域书签指向同一个文件夹 |

- **位置怎么定的**：Mac 记在 `~/.kairos/ledger-location.json`，**Node 侧 `ledger/paths.js` 读同一个文件**——being 跑 CLI 和 Kairos 必须写同一份账本。手机在沙盒里存绝对路径没意义，所以「关联」是书签。
- **换位置**：两份账本按 id 并集（`KairosLedgerUnion`：同一条留改得晚的那版、房间号和 human 标记不丢、墓碑和改动谁晚听谁的），并之前两份都备份到 `~/.kairos/backups/`。手机接上时把本机独有的条目推进自己的 outbox，Mac 一收就上桌。
- **关联之后规则一个字没变**：账本只有 Mac / being 写，手机只写自己的 outbox。见下一节。

## 以后：多设备

iCloud 不是服务器，是文件同步器：没有锁、没有原子性、没有顺序。两台设备写同一个文件，它不合并，挑一个赢家或者留个 `xxx 2.json`。让所有设备平等读写同一个账本，一次就能撞出好几条真冲突。

所以形状是不对称的，拓扑和 git 一样——各写各的，显式合并：

- **每个文件一个写者。** 每台设备只写自己的 `outbox/<设备id>.json`（追加、带 `seq`），永不冲突，多几台都无所谓。
- **合并者有且只有一个。** 谁把 outbox 并进账本、记水位，必须显式选定，不能靠「谁醒着谁合」。
- **算法不抄 git。** 三方 diff + 让人解冲突在手机上做不到。用的是字段级 `fields` + `lastWriter` 人工优先（`KairosOutboxEntry.merged(onto:)`）。

派生文件（房间日志、已读游标、outbox、锁）一律不进 iCloud。锁尤其不能进：一把陈旧的锁从别的设备飘过来，只会挡住本机唯一的写入者。

## 沿革

| 版本 | 时间 | 换了什么 |
|---|---|---|
| v1 | 2026-08-24 | 本地快照 + 显式同步 + 对账信封。状态藏在对话里，双写无规则 |
| v2.2 | 2026-09-07 | 换地基：文件是账本，消息是通知。每条待办一个渠道 |
| v2.3 | 2026-09-07 | 账本怎么写定死成五句规则；渠道 = being 房间，同一个 session_id |
| 手机单机 | 2026-09-08 | iCloud 同步下架。账本进手机沙盒，Loom 是唯一的线，三条通道各自房间 |
| v2.4 | 2026-09-10 | 场景感知（正文只剩人话，回执走 SSE meta）；账本位置可选：两台都能单机，iCloud 变成可选的关联 |
| v2.5 | 2026-09-14 | 账本 CLI（ledger/）随包分发；发布形态定为 Mac 主端 + 手机伴侣 |
| v2.5.1 | 2026-09-15 | 发布卫生：证据闸对齐两条通道（CLI set/create + Swift 账本轮）、tests 不进对外快照 |
| v2.5.2 | 2026-09-15 | mail/inbox 整线下线，Kairos 就是一个 todo；BEING-RULES v3；KairosTokens 全量化排版 |

