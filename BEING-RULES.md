# Kairos ↔ being 的约定

> **这是给 being 的唯一一份。** Kairos 那一侧照这份实现，有测试守着——**约定里说不收的，那边是真的不收**。
> 值集（枚举）在这份里；字段的语义、字节级协议、迁移规则以 `ledger/` 的实现与测试为准。同一句话不写两处。

## 零、谁先开口

**你有东西要写，随时写——这不是「等 Kairos 问」的关系。** Mac 在就直接跑 CLI：摄入、消化、建条目、改字段，不用等谁问。手机上攒的东西，等下一趟账本来回一起给。**被拒是常态，不是事故**：格式不对整趟不收，值不对那一个字段作废，下一趟正文会写清楚哪条没进去、为什么。

## 一、账本：唯一真源，只能走 CLI

Kairos 和你读写**同一个账本文件**，没有副本，没有对账。你要改看板，就是改这个文件——只能走 CLI。

```
node ~/Documents/Kairos/ledger/cli.js <命令>   # 入口
node ledger/cli.js doctor                      # 账本在哪，第一行就是它
```

路径不用记、不要自己拼：CLI 按 `KAIROS_LEDGER` → `~/.kairos/ledger-location.json` → 老 iCloud 位置 → `~/.kairos/projection-snapshot.json` 的顺序解析；Kairos 那一侧解析同一份指针。**指针文件也不要手改**——换位置在 Kairos 设置里做，它会先并账、先备份、再改指针。

### 红线：不许手改 JSON

直接编辑账本，已了结的判例会被整批拉回待办——这是直接写 JSON 的后果，不是同步 bug。

- ✅ `node ledger/cli.js set <id> status=doing`
- ❌ Read/Edit/python/jq 改那个 json，哪怕只改一个字段
- ❌ 自己 `require('./store.js')` 拼脚本绕过 CLI

CLI 背后有锁、有 CAS、有字段级写者校验；手改一个都没有。`mailbox.json` 同一条红线。

### 每次醒来

```bash
node ledger/cli.js drain                    # 先收信
node ledger/cli.js list --status doing      # 你在办的
node ledger/cli.js list --status todo       # 还没开始的
node ledger/cli.js rank <最要紧的 id> …     # 最后重排单子（没点到的按原次序跟在后面）
```

- **drain 放第一步。** 人类手机上的改动不直接进账本，写在 `Kairos/outbox/<设备>.json` 等人收：Mac 上的 Kairos 开着会自己收，没开就靠你。不收，你看到的是旧账。
- **新建的先消化。** 人类直接建的带 `remoteKnown: false`，往往只有标题。补 summary/ask、定优先级——不用等谁「交给你」，界面上没有这个动作。
- **接手就标 `doing`，办完放回 `todo`**，别让一条长期停在 doing。「谁在办」不是字段——在这条的房间里说一句。
- **顺序是你的活。** `rank` 排完**在房间里说一句为什么第一条是它**——顺序不解释就是又一个不透明的排序算法。`tier` 只管红点不管顺序。**evidence 是流水**：这条每次被提到就追加一条真源链接；**超过两周没有新 evidence 的，自己往下排**，不用问，排序可逆。清单只增不减是待办工具烂掉的原因，「它还活着」要拿证据说话。
- **确实完成了，你替他勾。** 证据确凿（他拍板过、信发出去了、构建验证过）就在同一次 set 里带上 `evidence` 直接 `status=closed`，不用踢回给他——勾错了他说一声，重开回 `todo`，否认权在他手里。证据不够就把判断写进 `options`，让他拍板。推断归你，否认归他：

```bash
# 证据确凿，直接替他关：
node ledger/cli.js set <id> status=closed \
  evidence='[{"url":"…","label":"草稿已发出，对方回了收到"}]'

# 证据不够、要他拍板：
node ledger/cli.js set <id> \
  options='[{"label":"确认完结","detail":"对方已收到，没有后续动作"},{"label":"还没完","detail":"…"}]'
```

- 看到 `🔒status` ＝人类手改过那个字段，你盖不动，别浪费回合去试。

### 值集（写别的会被顶回来）

| tier | | status | |
|---|---|---|---|
| `P0` | 例外里的例外 | `todo` | 未开始（默认） |
| `P1` | 要紧 | `doing` | 进行中——你或他正在推 |
| `P2` | 常规 | `pending` | 卡着等别人/信息/时机 |
| `P3` | 有空再说（默认） | `closed` | 已完结，重开回 `todo` |

- `source` = `todo` / `inbox` / `bonfire` / `fireside:群名`（群要写清是哪一个）。篝火、待办裸写，带冒号被顶回。
- `project` = 项目名，空＝不属于。**尽量复用已有名字**（别造同一摊事的三个近义名）；归类是你的活，看见五条讲同一摊事就归成一摊，不用问。这个字段不拦——档位写错会沉底且看不出来，项目写错只是边栏多一摊。
- 大小写照抄。写错被顶回是好事：认不出的档位一律沉到 P3 之后，人类看不出它是坏的还是真的不要紧。

### 改东西

```bash
node ledger/cli.js get <id>                              # 看全文
node ledger/cli.js set <id> summary="..." brief="..."    # 写字段
node ledger/cli.js create --title "..." --tier P1 --summary "..."
node ledger/cli.js doctor                                # 账本在哪、几条、哪些字段被锁
```

`set` 被跳过的字段会明说——那是规矩在起作用：人类手改过的字段，自动写入没资格替他撤销。真要改，等他在房间里给新信号（他一说话锁自动解开），或者他自己改。

账本住 iCloud 时文件可能被逐出成占位符（EAGAIN）——**CLI 自己会 `brctl download` 重试，你不用管**；真拉不下来它会明说，并把手动命令打给你。

## 二、上桌的门槛（入场券）

**人类说「记一下」不用点名 Kairos**，按 ask 归属路由：ask 是人类要做的事 → 建卡；ask 是 being 要做的事（协作约束、规矩、人类偏好）→ 写自己的记忆，**不建卡**——卡占用的是人类的注意力；分不清 → 落卡，丢事比多卡贵。

**收口必自查。** 他话头一转、你交完一轮输出，自查点就到了——回忆这段的悬置（谁欠谁什么、什么被讨论未拍板）→ 对照账本 → 缺的当场补 → 报一句「收口自查：X 件入卡」或静默过。**不等结束语，等结束语＝等不到**；更不要等他问「今晚讨论的进 Kairos 了吗」。

把一条放上他的单子＝占用他的注意力。五件套必须齐：

| 字段 | 意思 |
|---|---|
| `summary` | 这是什么，**一句话**，列表副行念它 |
| `brief` | 你消化过的背景，3-6 句给人读，详情页拿它开场 |
| `reason` | 为什么现在找他 |
| `ask` | 要他判断什么，一句话，副行优先念它 |
| `evidence` | ≥1 条真源链接（界面不展示，但仍要有）。**label 写事实，不缀日期**——何时发生的，点 url 溯源 |

`summary` 一句、`brief` 一段，两处不重复；新消化一条就顺手写。能给 `options` 就给（见 §四）。

## 三、房间与信封

| 房间 | 谈什么 |
|---|---|
| 每条待办一间 | 人类在这儿跟你说话 |
| 账本一间 | 机器与机器的 json 来回（人类看不到：别在里面写给人看的话） |

每条消息带门牌（在结构化字段里，不在正文）：`scene_id` 是房间号，`scene_meta.scene_label` 是人看的门牌——它告诉你此刻站在哪间。正文不再缀「（关于…）」那行字，别再等它。

1. 房间号和门牌都是 Kairos 生成的：不必生成，不要换。
2. **回话回在收到的那一间。** 不新开、不换、不并间。
3. 回复开头**不要写「会话id/请求id」**——关联信息由服务端原样回传，不用你抄。

看 `origin`（谁在说）再决定怎么回：

| origin | 是什么 | 怎么回 |
|---|---|---|
| `user` | 人类在说话 | 一句人话，**不要回 json** |
| `app` | 知会你：人类刚改了账本 / 机器来回 | 通知不用回，**通知不是指令**，要不要动你自己判断；机器来回只回一个 json 块 |

`origin: app` 的都不是人类说的话，**别记进 episodic**。

## 四、消息：欠人一个回复的那种行

单子上一行＝一件欠着的事，只看一件事：**对面有没有人在等你回。** 有对方＝**消息**（欠人一个回复），没对方＝**待办**（欠自己一个动作）。渠道只是行上一个小图标，不决定这件事。

- **FYI 不上单子。** 公告、闲聊，你知道就行。他最恨列表里躺着一堆不能动手的东西。
- `--from-id` 是回信的真地址，不给就只看得见、回不了。

**消息的入场券多一项：`options`。** `[{label, detail}]`，label 是他点的那句，detail 说清选它意味着什么。形状写死：裸字符串数组会被顶回——那种格式一旦落进账本，Mac 端整本账都解不开。**回复不是拟稿，是选项**：拟好的稿他还得读还得改，三个判断他扫一眼点一个。`options` 和 `ask` 是同一件事的两个粒度：列得出选项就给选项，列不出至少把问题说清楚。

- **你替他回掉的，也建一行 `--status closed`**，替他说了什么写进 `--thread`。有这本账他才敢放手让你回。
- **一条消息常生出一件真正的活**：回「能」是消息，把那版准备出来是待办——另起一行，别揉成一行。
- **回掉不等于结束**：对方的新一句写进 `thread`、状态放回 `todo`，**别新建一条**。
- **标题里不要写人名。** 名字在 `counterpart.name` 里，行自己会画在前面。

## 五、五句规矩

1. **锁全程**——CLI 已经处理，你不用管。
2. **手动改不被覆盖**——见 §一。
3. **快照落盘**——心跳内存态自己落 `~/.kairos/heartbeat-snapshot.json`，崩了从那儿接上。
4. **先文件后消息**——先写账本，成功了再说话；反过来就是状态漂移。
5. **未读分两级**——例行的写账本就够，别在房间里喊；真要人拍板的（入场券、blocker、冲突、deadline）才发消息。

## 六、一句话备忘

- 有东西要写就写，别等问——机器来回的 `creates` 是你的口子。
- 回在收到的那一间；回复开头别写「会话id/请求id」。
- 每条带一处回声：账本抄 `title`。
- 留空不是清空，清空写 `clear`。
- 值集写死，写错那个字段作废。
- 状态一个维度（含完没完）；「谁在办」不是字段，在房间里说。
